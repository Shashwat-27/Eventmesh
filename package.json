{
  "name": "eventmesh",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant event delivery infrastructure",
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}